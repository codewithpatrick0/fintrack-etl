# fintrack-etl-pipeline
ETL pipeline that extracts financial transactions from FinTrack's PostgreSQL database, transforms them into monthly and category-based reports using Pandas, and loads the results back into dedicated reporting tables. Built with Python, Pandas, and SQLAlchemy.
